/target
*.pdb

# Local environment / connection strings — never commit a DATABASE_URL.
.env
.env.*
!.env.example

# Claude Security scan reports are timestamped working output, not source.
CLAUDE-SECURITY-*/
